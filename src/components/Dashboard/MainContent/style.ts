import styled from "styled-components";

export const Container = styled.main`
    width: 100%;
    height: 100%;
    display: flex;
    gap: 2rem;

    #balance{
        @media (max-width: 900px){ 
            margin-bottom: 1rem;
        }
    }

`